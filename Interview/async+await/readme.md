## 异步
callback promise generate async/await

## co
tj koa

// foo().then(() => {
    //   Promise.then(() => {
    //     Promise.then(() => {
    //       Promise.then(() => {

    //       })
    //     })
    //   })
    // })